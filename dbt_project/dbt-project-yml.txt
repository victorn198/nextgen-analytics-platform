# dbt_project.yml - dbt Project Configuration

name: 'sales_analytics'
version: '1.0.0'
config-version: 2
profile: 'default'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sales_analytics:
    materialized: view
    staging:
      materialized: view
      tags: ["raw_data"]
    intermediate:
      materialized: table
      tags: ["business_logic"]
    marts:
      materialized: table
      tags: ["analytics"]

seeds:
  sales_analytics:
    +quote_columns: false

vars:
  start_date: '2023-01-01'
  lookback_days: 90

on-run-start: "{{ execute_notification('Pipeline started') }}"
on-run-end: "{{ execute_notification('Pipeline completed') }}"

require-dbt-version: [">=1.7.0", "<2.0.0"]