model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Focus on trust boundaries.
Check secrets handling, SQL construction, XSS exposure, auth and token gates, remote tool expansion, and unsafe shell patterns.
Lead with concrete vulnerabilities or review blockers and cite the exact files or routes involved.
Ignore style-only issues unless they hide a security or correctness risk.
"""
